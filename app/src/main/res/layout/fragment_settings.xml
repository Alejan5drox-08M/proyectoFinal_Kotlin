<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center">

    <!-- Switch para cambiar el tema -->
    <Switch
        android:id="@+id/switchTema"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Modo Oscuro"
        android:textSize="18sp"
        android:padding="10dp" />

    <!-- Texto para mostrar el saldo del usuario -->
    <TextView
        android:id="@+id/textSaldo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Saldo: €50.00"
        android:textSize="20sp"
        android:textStyle="bold"
        android:padding="15dp"
        android:gravity="center" />

    <Button
        android:id="@+id/btnGooglePay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#4CAF50"
        android:text="Pagar con Google Pay" />

    <!-- Botón para cerrar sesión -->
    <Button
        android:id="@+id/btnCerrarSesion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:backgroundTint="#F44336"
        android:padding="12dp"
        android:text="Cerrar Sesión"
        android:textColor="@android:color/white"
        android:textSize="18sp" />

    <!-- (Opcional) Botón para recargar saldo -->
    <Button
        android:id="@+id/btnRecargarSaldo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Recargar Saldo"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        android:padding="12dp"
        android:layout_marginTop="10dp"
        android:visibility="gone"/> <!-- Oculto hasta que lo implementes -->

</LinearLayout>
